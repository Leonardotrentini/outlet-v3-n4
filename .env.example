# Meta Conversions API (configurar na Vercel → Settings → Environment Variables)
META_PIXEL_ID=881981060699779
META_ACCESS_TOKEN=

# Rotador round-robin (obrigatório em produção)
# Opção 1: Vercel KV (Storage → Create → KV → link ao projeto)
# Opção 2: Upstash Redis (Marketplace → Upstash)
KV_REST_API_URL=
KV_REST_API_TOKEN=
